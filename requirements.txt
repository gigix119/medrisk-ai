# Runtime dependencies for the MedRisk AI API.
# Pinned to the exact versions verified working together (see README "Dependencies").
#
# fastapi              - web framework / routing / OpenAPI docs
# uvicorn[standard]    - ASGI server that runs the FastAPI app
# pydantic             - request/response/data validation (v2)
# pydantic-settings    - typed settings loaded from environment / .env
# python-dotenv        - reads .env files for pydantic-settings in local dev
# sqlalchemy           - async ORM (2.0 typed style) used for all DB access
# asyncpg              - async PostgreSQL driver used by SQLAlchemy
# alembic              - database schema migrations
# pyjwt                - JWT encoding/decoding for access + refresh tokens
# pwdlib[argon2]       - Argon2 password hashing
# python-multipart     - required by FastAPI/Starlette for form parsing (OAuth2 login form)
# email-validator      - backs pydantic's EmailStr validation

fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
sqlalchemy==2.0.51
asyncpg==0.30.0
alembic==1.14.1
pyjwt==2.13.0
pwdlib[argon2]==0.2.1
python-multipart==0.0.32
email-validator==2.3.0
